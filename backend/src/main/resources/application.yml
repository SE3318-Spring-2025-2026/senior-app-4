spring:
  application:
    name: spms-backend

server:
  port: 8081

github:
  client-id: YOUR_REAL_GITHUB_CLIENT_ID
  client-secret: YOUR_REAL_GITHUB_CLIENT_SECRET
  redirect-uri: http://localhost:8081/api/v1/auth/github/callback

token:
  secret: spms-issue-7-dev-secret
  expiration-seconds: 3600

supabase:
  url: placeholder-supabase-url
  service-key: placeholder-supabase-service-key