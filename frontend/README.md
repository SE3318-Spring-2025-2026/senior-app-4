# Frontend Notes

Project-wide setup instructions now live in the repository root `README.md`.

Use that file for:

- frontend and backend startup steps
- GitHub OAuth setup
- student login test flow
- troubleshooting

This frontend still runs with:

```bash
npm install
npm run dev
```

Required local frontend environment:

```bash
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
```
